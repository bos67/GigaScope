# Runtime dependencies for GigaScope distribution.
# Versions are bounded to compatible major lines; use a lock file for production.
neo4j>=5.20,<7
starlette>=0.37,<2
uvicorn>=0.30,<1
httpx>=0.27,<1
psutil>=5.9,<8
